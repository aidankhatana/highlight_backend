{
  "name": "highlight_backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required for backend'",
    "migrate": "npx prisma migrate dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@prisma/client": "^6.3.0",
    "prisma": "^6.3.0",
    "pg": "^8.11.0"
  },

  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
